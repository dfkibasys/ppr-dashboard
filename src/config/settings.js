// Add AAS idShort of asset to be excluded from Control Components page
export const EXCLUDED_ASSETS = [];

// Set amount of assets to be loaded per page (can be affected by EXCLUDED_ASSETS)
export const PAGE_SIZE = 8;
